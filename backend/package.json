{
  "name": "sanctuaire-sante-backend",
  "version": "1.0.0",
  "private": true,
  "description": "API du portail patient Sanctuaire Santé",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest --runInBand",
    "test:ci": "jest --runInBand --ci --reporters=default",
    "lint": "eslint \"src/**/*.js\" \"tests/**/*.js\""
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2",
    "pg": "^8.12.0"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^7.0.0"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
